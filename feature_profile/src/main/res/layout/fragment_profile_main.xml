<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.example.instagram_clone_clean_architecture.feature.profile.presentation.main.ProfileMainViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            layout="@layout/fragment_profile_main_user_profile"
            android:visibility="@{viewModel.stateLiveData.postLoading || viewModel.stateLiveData.profileLoading? View.GONE : View.VISIBLE}"
            android:id="@+id/user_profile_container"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/user_post_container"
            bind:userProfile="@{viewModel.stateLiveData.userProfile}" />

        <androidx.recyclerview.widget.RecyclerView
            android:visibility="@{viewModel.stateLiveData.postLoading || viewModel.stateLiveData.profileLoading? View.GONE : View.VISIBLE}"
            android:id="@+id/user_post_container"
            android:clipToPadding="false"
            android:scrollbars="vertical"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@+id/user_profile_container"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:spanCount="3"
            app:listData="@{viewModel.stateLiveData.userPosts}"
            tools:listitem="@layout/fragment_profile_main_user_posts_grid_view_item"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>